services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8710:8710"
    environment:
      - APP_NAME=API Auditoria
      - DEBUG=true
    restart: unless-stopped
